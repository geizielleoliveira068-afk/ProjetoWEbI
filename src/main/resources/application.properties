# ============ CONFIGURAÇÃO BÁSICA ============
spring.application.name=ProjetoWEbI
server.port=8080

# ============ BANCO DE DADOS POSTGRESQL ============
# MUDEI para um banco específico do seu projeto
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=191019
spring.datasource.driver-class-name=org.postgresql.Driver

# ============ JPA/HIBERNATE ============
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ============ DESATIVA SEGURANÇA COMPLETAMENTE ============
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# ============ CONFIGURAÇÕES OPCIONAIS ============
# JWT (se for usar depois)
#jwt.secret=minhaChaveSecretaSuperSeguraComPeloMenos32Caracteres
#jwt.expiration=86400000
